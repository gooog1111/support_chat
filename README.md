# 💬 Чат технической поддержки (локальная сеть)

## Внимание этот Readme.md сгенерирован ИИ

Простой и (не очень) безопасный чат для организации технической поддержки с разделением на клиентский и административный интерфейсы. Работает на Apache, Nginx или IIS без использования БД и Node.js.

![Пример интерфейса](https://github.com/gooog1111/support_chat/blob/main/chat/assets/images/2025-03-17_06-42-01.png)

## 🚀 Особенности
- **Клиентская часть**  
  📤 Отправка сообщений и изображений  
  📅 История переписки  
  🔄 Автоматическое восстановление сессии  
  👤 Уникальный идентификатор сессии для каждого клиента
- **Административная панель**  
  🛠 Управление чатами (открытие/закрытие/очистка)  
  🔍 Просмотр метаданных клиентов (IP, имя ПК, Kerberos)  
  📊 Статусы чатов (Открыт, В работе, Закрыт)  
  🔄 Автоматическое обновление списка чатов
- **Безопасность**  
  🔒 Защита от XSS и CSRF  
  🔑 Система сессий и авторизации  
  📁 Хранение данных в JSON-файлах  
  🔄 Регулярная очистка неактивных сессий
- **Адаптивность**  
  📱 Оптимизирован для мобильных устройств

## 📋 Требования
- **PHP**: 7.4+
- **Веб-сервер**: Apache, Nginx или IIS
- **Модули PHP**: fileinfo, json, session

---

## ⚙️ Установка

### 🪟 Windows

#### 1. **IIS + PHP**

1. **Установка IIS**:
   - Откройте "Панель управления" → "Программы" → "Включение или отключение компонентов Windows".
   - Найдите "Службы IIS" и установите галочку напротив "Веб-сервер (IIS)".
   - Нажмите "ОК" и дождитесь завершения установки.

2. **Установка PHP**:
   - Скачайте PHP с [официального сайта](https://windows.php.net/download/).
   - Распакуйте архив в `C:\PHP`.
   - Добавьте `C:\PHP` в переменную окружения `PATH`:
     - Откройте "Система" → "Дополнительные параметры системы" → "Переменные среды".
     - В разделе "Системные переменные" найдите `Path`, выберите "Изменить" и добавьте `C:\PHP`.

3. **Настройка IIS**:
   - Откройте "Диспетчер IIS".
   - Создайте новый сайт:
     - Укажите корневую папку как `C:\path\to\chat`.
     - Настройте обработчик для PHP:
       - В "Обработчики сопоставления" добавьте новый обработчик:
         - Запрос: `*.php`
         - Исполняемый файл: `C:\PHP\php-cgi.exe`
   - Убедитесь, что PHP работает:
     - Создайте файл `info.php` в корневой папке с содержимым:
       <?php phpinfo(); ?>
     - Откройте в браузере `http://localhost/info.php`. Если вы видите информацию о PHP, значит, он работает.

4. **Установка прав**:
   - Дайте полные права на папку `chat` пользователю `IIS_IUSRS`:
     - Правой кнопкой мыши на папке → "Свойства" → "Безопасность" → "Изменить" → Добавьте `IIS_IUSRS` и дайте полные права.

#### 2. **Apache + PHP**

1. **Установка Apache и PHP**:
   - Используйте [XAMPP](https://www.apachefriends.org/) или [WAMP](https://www.wampserver.com/).
   - Скачайте и установите XAMPP или WAMP, следуя инструкциям на сайте.

2. **Копирование проекта**:
   - Скопируйте папку `chat` в корневую директорию веб-сервера (например, `C:\xampp\htdocs\chat`).

3. **Настройка виртуального хоста**:
   - Откройте файл `httpd-vhosts.conf` (обычно в `C:\xampp\apache\conf\extra`).
   - Добавьте:
     <VirtualHost *:80>
         DocumentRoot "C:/xampp/htdocs/chat"
         ServerName chat.local
         <Directory "C:/xampp/htdocs/chat">
             Options Indexes FollowSymLinks
             AllowOverride All
             Require all granted
         </Directory>
     </VirtualHost>
   - Перезапустите Apache через панель управления XAMPP или WAMP.

4. **Проверка работы PHP**:
   - Создайте файл `info.php` в папке `chat` с содержимым:
     <?php phpinfo(); ?>
   - Откройте в браузере `http://chat.local/info.php`. Если вы видите информацию о PHP, значит, он работает.

---

### 🐧 Linux

#### 1. **Apache + PHP**

1. **Установка Apache и PHP**:
   - Обновите пакеты и установите Apache и PHP:
     sudo apt update
     sudo apt install apache2 php libapache2-mod-php php-common php-fileinfo

2. **Копирование проекта**:
   - Скопируйте папку `chat` в `/var/www/html/`:
     sudo cp -r chat/ /var/www/html/

3. **Настройка виртуального хоста**:
   - Создайте файл конфигурации:
     sudo nano /etc/apache2/sites-available/chat.conf
   - Добавьте:
     <VirtualHost *:80>
         DocumentRoot /var/www/html/chat
         ServerName chat.local
         <Directory "/var/www/html/chat">
             Options Indexes FollowSymLinks
             AllowOverride All
             Require all granted
         </Directory>
     </VirtualHost>
   - Включите сайт и перезапустите Apache:
     sudo a2ensite chat.conf
     sudo systemctl reload apache2

4. **Установка прав**:
   - Установите права на папку:
     sudo chown -R www-data:www-data /var/www/html/chat/
     sudo chmod -R 755 /var/www/html/chat/

5. **Проверка работы PHP**:
   - Создайте файл `info.php` в папке `chat` с содержимым:
     <?php phpinfo(); ?>
   - Откройте в браузере `http://chat.local/info.php`. Если вы видите информацию о PHP, значит, он работает.

#### 2. **Nginx + PHP**

1. **Установка Nginx и PHP**:
   - Обновите пакеты и установите Nginx и PHP:
     sudo apt update
     sudo apt install nginx php-fpm php-common php-fileinfo

2. **Копирование проекта**:
   - Скопируйте папку `chat` в `/var/www/html/`:
     sudo cp -r chat/ /var/www/html/

3. **Настройка Nginx**:
   - Создайте файл конфигурации:
     sudo nano /etc/nginx/sites-available/chat
   - Добавьте:
     server {
         listen 80;
         server_name chat.local;
         root /var/www/html/chat;

         index index.php index.html;

         location / {
             try_files $uri $uri/ =404;
         }

         location ~ \.php$ {
             include snippets/fastcgi-php.conf;
             fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
         }
     }
   - Включите сайт и перезапустите Nginx:
     sudo ln -s /etc/nginx/sites-available/chat /etc/nginx/sites-enabled/
     sudo systemctl restart nginx

4. **Установка прав**:
   - Установите права на папку:
     sudo chown -R www-data:www-data /var/www/html/chat/
     sudo chmod -R 755 /var/www/html/chat/

5. **Проверка работы PHP**:
   - Создайте файл `info.php` в папке `chat` с содержимым:
     <?php phpinfo(); ?>
   - Откройте в браузере `http://chat.local/info.php`. Если вы видите информацию о PHP, значит, он работает.

---

## 🔑 Настройка администраторов

Отредактируйте файл `includes/admins.php`:
'логин' => [
    'password' => password_hash('пароль', PASSWORD_DEFAULT),
    'name' => 'Имя администратора'
]

---

## 🖥 Использование
- **Клиентская часть**: http://ваш-сервер/client/
- **Административная панель**: http://ваш-сервер/admin/

Данные для входа по умолчанию:
- Логин: admin1
- Пароль: password

---
### Модули PHP

Для работы чата необходимы следующие модули PHP:
- **fileinfo**: Для работы с загрузкой файлов.
- **json**: Для работы с JSON-файлами.
- **session**: Для управления сессиями.

#### Как проверить, что модули подключены:
1. Создайте файл `info.php` в корневой папке проекта:
   <?php phpinfo(); ?>
2. Откройте его в браузере (например, `http://localhost/info.php`).
3. Найдите раздел "Loaded Modules" или "Загруженные модули". Убедитесь, что `fileinfo`, `json` и `session` присутствуют в списке.

#### Как подключить модули:
- **Windows (XAMPP/WAMP)**:
  - Откройте файл `php.ini` (обычно находится в `C:\xampp\php\php.ini` или `C:\wamp\bin\php\phpX.X.X\php.ini`).
  - Найдите строки, начинающиеся с `;extension=`, и раскомментируйте (уберите `;`) следующие строки:
    extension=fileinfo
    extension=json
    extension=session
  - Перезапустите Apache.

- **Linux**:
  - Установите модули, если они не установлены:
    sudo apt install php-fileinfo php-json php-session
  - Перезапустите веб-сервер:
    sudo systemctl restart apache2  # или nginx

Теперь PHP должен быть готов к работе с чатом. Убедитесь, что все модули активны, и приступайте к использованию!

---

## 🔒 Рекомендации по защите данных

### 1. **Защита от XSS (межсайтового скриптинг)**
- Все пользовательские данные проходят через функцию `sanitizeInput`, которая удаляет HTML-теги и экранирует специальные символы.
- Используйте `htmlspecialchars` для вывода данных в HTML.

### 2. **Защита от CSRF (межсайтовая подделка запроса)**
- Для каждой формы используется CSRF-токен, который проверяется на стороне сервера.
- Токен генерируется при каждой сессии и хранится в `$_SESSION`.

### 3. **Защита сессий**
- Используйте `session_regenerate_id(true)` для предотвращения фиксации сессии.
- Установите `cookie_httponly` и `cookie_secure` в настройках сессии для защиты от XSS и перехвата куки.

### 4. **Хранение данных**
- Все данные хранятся в JSON-файлах, которые защищены от прямого доступа через `.htaccess`.
- Регулярно проверяйте права доступа к файлам (755 для директорий, 644 для файлов).

### 5. **Защита от перебора паролей**
- Используйте `password_hash` и `password_verify` для хранения и проверки паролей.
- Ограничьте количество попыток входа (3 попытки в текущей реализации).

### 6. **Защита от несанкционированного доступа**
- Ограничьте доступ к административной панели через `.htaccess` или IP-фильтрацию.
- Используйте HTTPS для шифрования данных в производственной среде.

### 7. **Регулярное обслуживание**
- Регулярно обновляйте пароли администраторов.
- Мониторьте логи: `logs/error.log`.
- Удаляйте неактивные сессии через 24 часа.

---


## 📂 Структура проекта
```
chat/
├── admin/               # Панель администратора
│   ├── clear_chat.php   # Очистка чата
│   ├── close_chat.php   # Закрытие чата
│   ├── dashboard.php    # Основная панель управления
│   ├── get_chat_list.php# Получение списка чатов
│   ├── get_messages.php # Получение сообщений чата
│   ├── index.php        # Перенаправление на страницу входа
│   ├── login.php        # Страница входа для администратора
│   ├── logout.php       # Выход из системы
│   ├── send_message.php # Отправка сообщений от администратора
│   ├── update_chat_status.php # Обновление статуса чата
│   ├── update_hostnames.php   # Обновление имен ПК клиентов
│   └── update_profile.php     # Обновление профиля администратора
├── admin_online/        # Файлы для отслеживания онлайн-статуса администраторов
├── assets/              # Ресурсы проекта (стили, скрипты, изображения)
│   ├── css/             # Стили
│   │   ├── admin.css    # Стили для административной панели
│   │   ├── all.min.css  # Минифицированные стили (например, FontAwesome)
│   │   ├── index.php    # Защита от прямого доступа
│   │   └── styles.css   # Основные стили для клиентской части
│   ├── images/          # Изображения
│   │   └── index.php    # Защита от прямого доступа
│   ├── js/              # JavaScript-скрипты
│   │   ├── admin.js     # Скрипты для административной панели
│   │   ├── index.php    # Защита от прямого доступа
│   │   └── script.js    # Скрипты для клиентской части
│   ├── webfonts/        # Шрифты
│   │   └── index.php    # Защита от прямого доступа
│   └── index.php        # Защита от прямого доступа
├── chats/               # История сообщений (JSON-файлы)
│   └── index.php        # Защита от прямого доступа
├── client/              # Клиентская часть
│   ├── get_messages.php # Получение сообщений для клиента
│   ├── get_online_admins.php # Получение количества онлайн-администраторов
│   ├── index.php        # Основной интерфейс клиента
│   ├── restore_session.php # Восстановление сессии клиента
│   └── send_message.php # Отправка сообщений от клиента
├── clients/             # Данные о клиентах (JSON-файлы)
│   └── index.php        # Защита от прямого доступа
├── includes/            # Системные скрипты и конфигурации
│   ├── admins.php       # Данные администраторов (логины, пароли, имена)
│   ├── config.php       # Основные настройки проекта
│   ├── functions.php    # Вспомогательные функции (санитизация, CSRF-токены)
│   ├── index.php        # Защита от прямого доступа
│   ├── session.php      # Управление сессиями
│   ├── storage.php      # Функции для работы с данными (сохранение, обновление)
│   └── upload_functions.php # Функции для загрузки файлов
├── logs/                # Логи ошибок
│   ├── error.log        # Файл логов
│   └── index.php        # Защита от прямого доступа
├── uploads/             # Загруженные файлы (изображения)
│   └── index.php        # Защита от прямого доступа
└── index.php            # Главный файл для перенаправления на клиентскую часть
```
---

## 👥 Разработка

Автор: [gooog1111](https://github.com/gooog1111/)

Лицензия: MIT License

[![MIT License](https://img.shields.io/badge/License-MIT-green.svg)](https://opensource.org/licenses/MIT)

---

> ⚠️ **Важно!** Перед использованием в продакшене проведите аудит безопасности и настройте HTTPS.

